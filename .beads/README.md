# Beads archive (migration in progress)

Issue tracking has moved to **GitHub Issues** on `timkindberg/formframe`.

- Triaged migrate map: [`scripts/bd-to-github-migration-map.json`](../scripts/bd-to-github-migration-map.json)
- Allowlist used: [`scripts/migrate-allowlist.txt`](../scripts/migrate-allowlist.txt)
- Migrator: [`scripts/migrate-bd-to-github.py`](../scripts/migrate-bd-to-github.py)

`.beads/` is retained temporarily while parallel agents still have open beads.
Do not treat this directory as the source of truth for new work.
