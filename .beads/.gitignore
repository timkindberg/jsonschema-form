# SQLite databases
*.db
*.db-journal
*.db-wal
*.db-shm

# Daemon runtime files
daemon.lock
daemon.log
daemon.pid
bd.sock

# Legacy database files
db.sqlite
bd.db

# Local sync/backup state (machine-specific, not for git)
backup/
push-state.json
.local_version
LOCK

# Keep JSONL exports and config (source of truth for git)
!*.jsonl
!metadata.json
!config.json
