{
  "name": "@jsonschema-form/input-jsonschema",
  "version": "0.0.0",
  "description": "JSON Schema front-end for @jsonschema-form/core — compiles JSON Schema into the neutral form tree (ADR 033)",
  "type": "module",
  "sideEffects": false,
  "engines": {
    "node": ">=18"
  },
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "development": "./src/index.ts",
      "import": {
        "types": "./dist/index.d.ts",
        "default": "./dist/index.js"
      },
      "require": {
        "types": "./dist/index.d.cts",
        "default": "./dist/index.cjs"
      }
    }
  },
  "files": [
    "dist",
    "src",
    "!**/*.test.ts",
    "!**/*.test.tsx",
    "!**/*.spec.ts",
    "!**/*TestUtils.ts"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts --clean --sourcemap --tsconfig ../../tsconfig.tsup.json",
    "test": "vitest",
    "test:watch": "vitest --watch",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "json-schema",
    "forms"
  ],
  "author": "Tim Kindberg",
  "license": "MIT",
  "peerDependencies": {
    "@jsonschema-form/core": "*"
  },
  "dependencies": {
    "json-schema-typed": "^8.0.1"
  },
  "devDependencies": {
    "@jsonschema-form/core": "*",
    "@jsonschema-form/input-conformance": "*",
    "typescript": "^5.3.3",
    "vitest": "^4.0.9"
  }
}
